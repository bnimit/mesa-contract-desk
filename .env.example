ANTHROPIC_API_KEY=sk-...          # Required — get from console.anthropic.com
# MESA_API_KEY=                   # Not needed yet — using local filesystem fallback
# ALPHA_VANTAGE_API_KEY=          # Optional — fallback if Yahoo Finance is down
